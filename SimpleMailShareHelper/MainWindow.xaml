<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SimpleMailShareHelper.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleMailShareHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SimpleMailShareHelper">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row="0"
            Text="Share a file to this app!"
            FontSize="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="0,0,0,10"/>

        <TextBlock 
            Grid.Row="1"
            x:Name="FileNameTextBlock"
            Text="Waiting for a shared file..."
            FontSize="20"
            FontWeight="Bold"
            Foreground="RoyalBlue"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="20"/>
        <TextBlock 
            Grid.Row="2"
            x:Name="ErrorMessageTextBlock"
            Text="Thunderbird location..."
            FontSize="20"
            FontWeight="Bold"
            Foreground="RoyalBlue"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="20"/>
    </Grid>
</Window>
